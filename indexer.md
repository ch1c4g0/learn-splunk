
<p><h1>Splunk Indexer</h1></p>

<p>The indexer plays the main role in processing data it receives from forwarders. It parses and normalizes the data into field-value pairs, categorizes it, and stores the result as an event, this allows it to be easily searched and indexed.</p>
